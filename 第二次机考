//1 

/*#include<stdio.h>
main()
{
	int num,max,min;
	scanf("%d",&num);
	while(num<=0)
	scanf("%d",&num);
	max = num;
	min = num;
	while(num!=0)
	{
		scanf("%d",&num);
		if(num>0)
		{
			if(num > max)
			max = num;
			else if(num < min)
			min = num; 
		}
	}
	printf("%d",max - min);
	
	return 0;
}*/

//2
/*#include<stdio.h>
main()
{
	int num,count,b = 0,i = 0;
	scanf("%d",&num);
	count = num % 10;
	while(count != 0)
	{
		b += count*(int)pow(10,i);
		num /= 10;
		count = num % 10; 
		i++;
	}
	num /= 10;
	printf("%d",num + b);
	
	return 0;
} */


//3
/*#include<stdio.h>
#include<math.h>
main()
{
	char a;
	int num = 0,i = 0;
	while((a = getchar())!='\n')
	{
		if(a>='0'&&a<='9')
		{
			num += (int)pow(10,i)*(a - '0');
			i++;
		}
		else if(a == '*')
		;
	}
	printf("%d",num);
	
	return 0;
}*/


//4

/*#include<stdio.h>
main()
{
	char a;
	while((a = getchar())!='\n')
	{
		if(a>='a'&&a<='z')
		putchar(a-32);
		else if(a>='A'&&a<='Z')
		putchar('A'+'Z'-a);
		else if(a>='0'&&a<='6')
		putchar(a+3);
		else if(a>='7'&&a<='9')
		putchar(a-7);
	}
	
	return 0;
}*/
